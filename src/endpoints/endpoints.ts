/** @format */

const API_ENDPOINT = {
  AUTH: {
    REGISTER: '/api/auth/register',
  },
  PRODUCT: {
    ADMIN: {
      CREATE: '/api/admin/product/create',
    },
    DETAIL: '/api/product/detail?productId=',
  },
};

export default API_ENDPOINT;
